services:
  - type: web
    name: coolsciencegames-proxy
    env: node
    rootDir: proxy-server
    buildCommand: npm install
    startCommand: node server.js
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
